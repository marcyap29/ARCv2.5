# Large models
ARC MVP/EPI/assets/models/**
ARC MVP/EPI/assets/models/qwen/**
ARC MVP/EPI/assets/models/**/*.gguf
ARC MVP/EPI/assets/models/**/*.bin
ARC MVP/EPI/temp_models/**

cat >> .gitignore << 'EOF'

# Build & tool output
build/
.dart_tool/
.flutter-plugins*
**/DerivedData/

# CocoaPods (vendor binaries)
ios/Pods/

# iOS binary artifacts
*.ipa
*.xcarchive
*.a
*.dylib
*.so
*.o

# AI/ML Models (large binary files - should be downloaded or use LFS)
*.gguf
*.bin
*.model
*.weights

# Bundled frameworks (should be resolved via Pods/SPM, not vendored)
*.framework/
*.xcframework/

# Big media/archives that shouldn't be versioned
*.zip
*.tar
*.tgz
*.gz
*.mp4
*.mov

# Previously problematic
..bfg-report/
**bfg-report*/
**/.flutter-plugins-dependencies
.DS_Store

# Build cache
.buildlog/
